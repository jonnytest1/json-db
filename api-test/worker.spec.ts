describe("worker", () => {

    const serverPort = 23422
    it("loads from worker", async () => {


        fetch(`http://localhost:${serverPort}/json`, {
            headers: {

            }
        })

    })
})